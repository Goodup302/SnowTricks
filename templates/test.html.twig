{% extends 'base.html.twig' %}

{% block title %}Test{% endblock %}

{% block body %}
        {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_row(form.name) }}

        <button type="submit">submit</button>
        {{ form_end(form) }}
{% endblock %}
